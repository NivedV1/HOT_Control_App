# Python Port - HOT Control App

This folder contains an incremental Python port of the C++/Qt HOT Control App.

## Current status
See `progress.md` for step-by-step status.

## Quick start
1. Create and activate a virtual environment.
2. Install deps:
   `pip install -r requirements.txt`
3. Run:
   `python main.py`
